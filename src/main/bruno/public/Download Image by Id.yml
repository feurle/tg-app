info:
  name: Download Image by Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/webcontent/images/:id/download"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
