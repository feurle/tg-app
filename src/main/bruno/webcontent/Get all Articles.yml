info:
  name: Get all Articles
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/webcontent/articles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
