info:
  name: Delete Image
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/webcontent/images/:id"
  params:
    - name: id
      value: "3"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
