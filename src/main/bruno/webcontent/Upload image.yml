info:
  name: Upload image
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/webcontent/images"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/daniel/Downloads/E31.pdf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
