info:
  name: Get Vet Info
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/vetinfo"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
