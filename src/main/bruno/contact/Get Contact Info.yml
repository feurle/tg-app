info:
  name: Get Contact Info
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/contact/info"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
