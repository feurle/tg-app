info:
  name: Get Customer by Email
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/customer/email/max@example.com"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5