info:
  name: Get all Images
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/webcontent/images"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
